package manage;

import java.sql.Connection;
import java.sql.DriverManager;
import java.sql.ResultSet;
import java.sql.Statement;
import java.util.ArrayList;
import java.util.List;

import ems.Answer;

public class AnswerDTO {
	/*
	 * public AnswerDTO(int id, String name, int mark, boolean status, int
	 * questionId, String qname) { super(id, name, mark, status, questionId, qname);
	 * // TODO Auto-generated constructor stub }
	 * 
	 * public List<Answer> answerList() {
	 * 
	 * try { Class.forName("com.mysql.cj.jdbc.Driver");
	 * 
	 * Connection con =
	 * DriverManager.getConnection("jdbc:mysql://localhost/ExamManagementSystem",
	 * "root","minkoko");
	 * 
	 * String sql = "select * from answer";
	 * 
	 * Statement stm = con.createStatement(); ResultSet rs=stm.executeQuery(sql);
	 * List<Answer> answerList = new ArrayList<Answer>(); while(rs.next()) { Answer
	 * ans = new Answer(rs.getInt(1),rs.getString(2),rs.getBoolean(3),rs.getInt(4));
	 * answerList.add(ans); } con.close();
	 * 
	 * 
	 * }catch(Exception e) { e.printStackTrace(); }
	 * 
	 * return answerList();
	 * 
	 * 
	 * }
	 */
	
}
